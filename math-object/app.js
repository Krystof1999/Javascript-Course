// Math object

val = Math.PI;
val = Math.E;
val = Math.round(2.8); //3
val = Math.ceil(2.4); // 3
val = Math.floor(2.8); // 2
val = Math.sqrt(64); // 8
val = Math.abs(-3); // 3
val = Math.pow(8, 2); // 64
val = Math.min(2, 33, 4, 56, -2); // -2
val = Math.max(2, 3, 50); // 50
val = Math.random();

val = Math.random() * 20; // random number between 0 to 19
val = Math.random() * 20 + 1; // random number between 1 to 20
val = Math.floor(Math.random() * 20 + 1); // random number between 1 to 20 (whole numbers)
